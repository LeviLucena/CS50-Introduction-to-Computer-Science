#include <cs50.h>
#include <stdio.h>

int main(void)
{
     // Pega os números do usuário
     int x = get_int("x: ");

     // Pega os números do usuário
     int y = get_int("y: ");

    // Soma x com y
      printf("%i\n", x + y);
}